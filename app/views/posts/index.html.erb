<%=  @posts.each do |post| %>
  <div>
    <h2><%= link_to post.title, post%></h2>
    <p>
    Created at <%= post.created_at.strftime("%d %m %Y") %> by <%= post.user.email %>
    </p>
  </div>
<% end %>
